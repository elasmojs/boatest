try{
    /*
    const array1 = ['a', 'b', 'c'];

    //Not supported
    for (const element of array1) {
        console.log(element);
    }

    // expected output: "a"
    // expected output: "b"
    // expected output: "c"
    true;
    */
   false;
}catch(e){
    console.log(e);
    false;
}